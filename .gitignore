# Dependencies and local analyzer state.
node_modules/
.code-analysis/
.serena/
coverage/
*.map
.DS_Store

# Local reference clones; keep only their provenance manifest in the parent Git
# repository. The nested .git directory remains local reference metadata.
Inspiration/
inspirations/*
!inspirations/manifest.json

# The TypeScript sources are canonical; these are accidental compiler emits.
src/**/*.js
tests/**/*.js
scripts/**/*.js
vitest.config.js
